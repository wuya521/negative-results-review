<%- include('partials/head', { pageTitle: '关于' }) %>
<%- include('partials/nav') %>

<header class="page-header page-header--compact">
  <div class="container narrow-container">
    <p class="page-header__label">About / 关于本馆</p>
    <h1 class="page-header__title">一座为负结果保留尊严的档案馆</h1>
    <p class="page-header__sub">《负结果通讯》不是快讯流，而是一套长期保存失败经验、方法教训与研究情绪的馆藏系统。我们希望把那些通常会消失的研究碎片，整理成可以被再次阅读、引用与继承的公共记录。</p>
  </div>
</header>

<main class="section">
  <div class="container narrow-container about-shell">
    <section class="about-block fade-in">
      <h2>我们在做什么</h2>
      <p>这里收录实验未达预期、方法失灵、审稿扭曲、选题夭折，以及研究者在过程中真实承受过的情绪与判断。重点不在制造戏剧化叙事，而在保留那些本应成为后续研究路标的经验。</p>
      <p>因此，这个项目更像一座馆，而不是一个普通内容站。文章会被编目、赋予馆藏编号、附上馆方评语，并纳入专题策展与长期归档体系。</p>
    </section>

    <section class="about-block fade-in">
      <h2>馆藏标准</h2>
      <ul class="about-list">
        <li>保留真实失败的结构，而不是只保留结论。</li>
        <li>允许阴性结果、半成品复盘和被拒稿件进入公共视野。</li>
        <li>优先选择能为后来者节约时间、经费与情绪成本的内容。</li>
        <li>通过策展评语和期号体系，给作者清晰的归属与荣誉感。</li>
      </ul>
    </section>

    <section class="about-block fade-in">
      <div class="section-header section-header--compact">
        <h2 class="section-title">馆务团队</h2>
        <span class="section-en">CURATORIAL BOARD</span>
      </div>
      <% if (curatorBoard && curatorBoard.length) { %>
      <div class="curator-board curator-board--stacked">
        <% curatorBoard.forEach(function(curator) { %>
          <% if (curator.public_slug) { %>
          <a href="/curators/<%= curator.public_slug %>" class="curator-card curator-card--wide curator-card--link">
          <% } else { %>
          <article class="curator-card curator-card--wide">
          <% } %>
            <div class="curator-card__head">
              <span class="curator-card__badge"><%= curator.badge_label %></span>
              <span class="curator-card__role"><%= curator.public_title %></span>
            </div>
            <h3 class="curator-card__name"><%= curator.display_name %></h3>
            <% if (curator.bio) { %><p class="curator-card__bio"><%= curator.bio %></p><% } %>
            <div class="curator-card__meta">
              <span>@<%= curator.username %></span>
              <span><%= curator.public_slug ? '查看人物页' : '馆务成员' %></span>
            </div>
          <% if (curator.public_slug) { %>
          </a>
          <% } else { %>
          </article>
          <% } %>
        <% }) %>
      </div>
      <% } else { %>
      <p class="comments-empty">馆务团队信息尚未公开。</p>
      <% } %>
    </section>
  </div>
</main>

<%- include('partials/footer') %>
